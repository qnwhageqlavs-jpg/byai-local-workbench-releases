# 百应本地任务工作台员工使用手册

本手册面向第一次安装和日常操作百应本地任务工作台的员工。工作台运行在每个人自己的 Windows 电脑上，不共享本机任务、客户数据、执行记录或凭据。

## 一、使用前准备

开始前确认你已经具备：

- Windows 10 或 Windows 11 电脑；
- 管理员提供的 BYAI 配置信息；
- 当天有效的 CSMS Token；
- 任务计划 Excel；
- 对应的客户数据文件，支持 `.xlsx`、`.xls`、`.csv`；
- 园区对应的推广模块信息。

如果缺少 BYAI 配置、CSMS Token 或推广模块，不要猜测填写，应联系管理员。

## 二、第一次怎么下载和安装

### 1. 打开官方下载页

访问：<https://github.com/qnwhageqlavs-jpg/byai-local-workbench-releases/releases/latest>

该页面可以匿名访问，不需要 GitHub 账号。只使用这个公开仓库的 Release，不从聊天文件、网盘或陌生链接接收安装包。

### 2. 下载正确的文件

在 Release 页面找到 **Assets**，下载：

```text
BYAIWorkbench-版本号-Setup.exe
```

普通员工不需要下载：

- `Source code`；
- `BYAIWorkbench-版本号-windows-x64.zip`；
- `release-manifest.json`；
- `CHANGELOG.md`、`MIGRATION.md`、`KNOWN-LIMITATIONS.md`。

需要校验安装包时，可同时下载 `SHA256SUMS.txt`，由管理员核对 SHA-256。

### 3. 安装并启动

1. 双击 Setup 安装包。
2. 按安装向导完成安装。
3. 当前内部版可能显示“未知发布者”。先确认文件来自上述公开仓库并核对版本；来源不明时立即取消。
4. 安装完成后，从桌面或开始菜单打开“百应本地任务工作台”。
5. 工作台会在本机启动并打开浏览器页面。关闭浏览器页面不一定会结束本机程序。

默认程序目录：`%LOCALAPPDATA%\Programs\BYAIWorkbench\`

默认数据目录：`%LOCALAPPDATA%\BYAIWorkbench\`

## 三、第一次打开后的配置

### 1. 检查程序版本

进入“帮助与更新”，点击“检查更新”。该操作匿名读取公开 Release，不需要登录 GitHub，也不会自动安装。

### 2. 配置 BYAI

进入“系统设置”，点击“一键导入配置”。

- 本机有旧配置：工作台会读取并校验允许迁移的字段，再保存到 Windows 凭据管理器。
- 本机没有旧配置：页面会展开首次配置，填写管理员提供的 Client ID、Company ID、手机号、Access Token 和有效期。

保存后页面不会显示完整凭据。不要把 BYAI 配置文件、Token 或截图发送给无关人员。

### 3. 保存当天 CSMS Token

在“系统设置”中填写并保存当天 CSMS Token。页面显示“已保存”只代表已写入本机，不代表 CSMS 平台已经完成业务验证。

以下情况必须重新获取：

- Token 不是今天保存；
- 距保存时间超过 24 小时；
- 页面提示 Token 无效或时间异常；
- CSMS 重新登录后 Token 已变化。

### 4. 设置客户数据根目录

选择本机存放客户数据包的总目录并保存。保存目录不会读取客户数据，也不会自动扫描。

进入“数据准备”页面后，必须由员工点击“扫描目录”。工作台会递归检查目录中的支持文件，并按文件名主体匹配任务计划中的数据包名称。

### 5. 检查推广模块映射

工作台从任务名称中识别园区别名，并精确匹配启用的推广模块。缺失或同时命中多个模块时会停止。

不要模糊选择近似园区。按页面提供的“系统设置”入口维护正确的园区别名、模块名称和模块 ID。

## 四、每天的六步操作

### 第 1 步：开工准备

检查以下三项是否就绪：

- BYAI Access Token 在有效期内；
- CSMS Token 是当天保存且未超过 24 小时；
- 本机数据根目录可访问。

未就绪时，按页面提示进入“系统设置”修复。打开页面本身不会创建百应任务。

### 第 2 步：任务计划

1. 上传任务计划 Excel。
2. 确认四个核心字段：任务名称、数据包、话术、拨打周期。
3. 检查预览中的字段映射和每一行任务。
4. 确认整表无误后再导入。

导入计划只保存本机计划，不创建百应任务，也不回传 CSMS。

拨打周期可以使用明确日期或星期。日期和星期混用、含未知内容或无法得到执行日期时，系统会阻断。

### 第 3 步：数据准备

1. 点击“扫描目录”。
2. 检查计划引用的数据包是否唯一命中。
3. 将正确的数据版本绑定到计划或当天任务。

同名文件存在多个内容版本时，必须人工选择，不能依赖模糊匹配。扫描只登记安全元数据，不会自动上传客户数据。

### 第 4 步：任务配置

1. 读取“百应线路目录”，确认线路名称、ID、类型、场景和可用状态。
2. 为当天任务选择线路。移动线路可以多选，其他类型按页面规则单选。
3. 检查工作台识别出的园区别名和推广模块。
4. 检查最终任务名称、话术、数据包和线路。

任务名称通常使用：

```text
MMDD园区别名-任务描述
```

线路状态会在执行前再次读取。线路失效、推广模块缺失或任务名称无法识别时，不会进入外部执行。

### 第 5 步：执行前检查

本步骤只检查，不执行。

工作台会重新检查：

- BYAI 与 CSMS Token；
- 话术；
- 线路；
- 推广模块；
- 客户文件及其哈希；
- 百应后台是否已经存在完整同名任务。

检查通过后生成不可变快照。刷新页面仍会恢复同一条检查记录；跨天后必须重新检查。

发现同名百应任务时，系统会显示任务 ID 和状态并停止，禁止重复创建。

### 第 6 步：执行任务

只有员工在本页明确点击“执行任务”后，工作台才会依次执行：

1. 创建百应任务；
2. 反查并确认百应任务 ID；
3. 回传 CSMS 推广模块；
4. 导入客户数据；
5. 启动百应任务；
6. 验证启动结果并生成本地回执。

任一关键步骤失败，当前任务会停止，批次不会继续创建后续任务。不要连续点击按钮或重新上传规避停止状态。

## 五、第一次任务完成后怎么确认

不要只看页面出现“完成”。至少核对以下证据：

1. “任务记录”中该任务显示“已完成”。
2. 执行详情显示已经取得百应任务 ID。
3. 创建、验证、CSMS、客户导入、启动、启动验证等步骤显示成功。
4. 导入数量与本次有效、去重后的客户数量一致。
5. 到百应后台按任务名称或任务 ID 核对任务状态和进度。
6. 如 CSMS 只返回“已接收”但没有远端记录 ID，由管理员到 CSMS 后台独立核对。

“百应进度完成”不一定代表已经产生真实接通或话术对话。若正式验收包含通话效果，还应使用不会被平台拦截的内部号码验证响铃、接通、话术和结果回写。

## 六、任务记录怎么看

“任务记录”只读取本机账本，不会调用 BYAI 或 CSMS，也不会产生外部写入。

可以查看：

- 计划进度；
- 执行前检查记录；
- 真正的执行记录；
- 客户导入批次进度；
- 创建、CSMS、导入、启动和验证时间线；
- 本地回执和安全错误说明。

“仅完成执行前检查”不等于已经创建百应任务。“执行结果未确认”表示必须人工到后台核对，不能自动重试。

## 七、常见问题

### 页面显示今天没有任务

检查任务计划是否已导入、拨打周期是否包含今天、数据包是否已绑定。未来日期任务不会提前创建到百应。

### CSMS Token 显示不是今天保存

重新登录 CSMS 获取当天 Token，然后回到“系统设置”保存。不要重复使用跨日 Token。

### 看不到可用线路

先检查 BYAI Token，再重新读取线路目录。线路无效、已停用或接口读取失败时，不要手工猜线路 ID。

### 提示推广模块缺失

检查任务名称中的园区别名，再到“系统设置”维护精确映射。不要选择名称相近但不确定的模块。

### 提示发现同名任务

到百应后台核对页面显示的任务 ID 和状态。不得改名后重复创建，也不得直接删除原任务。

### 显示“部分成功”“结果未确认”或“需要人工复核”

停止操作，到百应、CSMS 和本机任务记录交叉核对。不要重复导入、重新上传或重新点击执行。

### 安装时提示程序正在运行

确认没有任务正在执行，关闭工作台页面，并允许安装器自动关闭 `BYAIWorkbench`。如仍无法关闭，取消安装，联系管理员处理。

## 八、更新和覆盖安装

1. 确认没有任务正在执行。
2. 进入“帮助与更新”检查新版本。
3. 点击“下载并准备更新”，等待校验完成。
4. 人工确认“安装并重启”。

覆盖升级只替换程序区，不删除本机任务、回执、设置和 Windows 凭据。更新失败时保留当前版本；不要从第三方链接补装未知文件。

## 九、隐私与求助

禁止上传或发送：

- Access Token、CSMS Token、Cookie、Authorization；
- 客户姓名、手机号和原始数据包；
- 本机数据库、完整数据目录或凭据文件；
- 包含上述内容的截图。

求助时只提供：

- 程序版本；
- 任务本地编号；
- 错误代码；
- 脱敏后的错误摘要；
- 发生时间。

官方下载页：<https://github.com/qnwhageqlavs-jpg/byai-local-workbench-releases/releases/latest>
